import { motion } from "framer-motion"; export function Scene0_Opening({ progress }: { progress: number }) { return ( <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="absolute inset-0 flex flex-col items-center justify-center text-center"> <motion.div initial={{ scale: 0.8, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} transition={{ delay: 1, duration: 2, ease: "easeOut" }} > <h1 className="text-6xl md:text-8xl font-black tracking-tighter mb-4 text-white">SBI VISHWAS</h1> <motion.p initial={{ opacity: 0, y: 20 }} animate={{ opacity: progress > 0.4 ? 1 : 0, y: progress > 0.4 ? 0 : 20 }} className="text-xl md:text-2xl text-indigo-300 font-light tracking-wide mb-2" > Trust is the Foundation. </motion.p> <motion.p initial={{ opacity: 0 }} animate={{ opacity: progress > 0.7 ? 1 : 0 }} className="text-sm md:text-lg text-white/50 tracking-widest uppercase" > An Autonomous AI Banking Operating System. </motion.p> </motion.div> </motion.div> ); }
